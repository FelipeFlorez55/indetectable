# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# TypeScript incremental build info
*.tsbuildinfo

# Test coverage
coverage/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.local

# Env (no secrets in this repo by design — see CLAUDE.md §2)
.env
.env.*

# Local deploy tooling — kept OUT of the public repo. No credentials (those live
# in ~/.aws), but it carries infra identifiers (S3 bucket, CloudFront distribution
# id, AWS profile) we don't want to publish.
scripts/deploy.sh
